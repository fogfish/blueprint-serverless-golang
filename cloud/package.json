{
  "name": "blueprint-serverless-golang",
  "version": "0.0.0",
  "dependencies": {
    "@aws-cdk/aws-apigateway": "*",
    "@aws-cdk/aws-lambda": "*",
    "@aws-cdk/core": "*",
    "@types/node": "14.14.13",
    "aws-scud": "*"
  },
  "devDependencies": {
    "@aws-cdk/assert": "*",
    "@types/jest": "^26.0.19",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "ts-node": "^9.1.1",
    "tslint": "^6.1.3",
    "typescript": "^4.1.3"
  },
  "scripts": {
    "tsc": "tsc --noEmit",
    "clean": "rm -Rf cdk.out && rm -Rf node_modules",
    "cdk": "npx cdk",
    "lint": "tslint -p tsconfig.json",
    "test": "jest --no-cache"
  }
}
